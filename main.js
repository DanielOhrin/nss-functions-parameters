const quarterValue = (numToQuarter) => {
    const numQuartered = numToQuarter / 4;
    return numQuartered;
}

// Invoke the function and store the return value
const quarterThirtyEight = quarterValue(38)

// Log the return value
console.log(quarterThirtyEight)